// Recursive functions work via call-before-definition or self-recursion.
fn factorial(n: int) -> int {
    if n == 0 { return 1; };
    return n * factorial(n - 1);
}

output(factorial(0));   // 1
output(factorial(1));   // 1
output(factorial(5));   // 120
output(factorial(10));  // 3628800
